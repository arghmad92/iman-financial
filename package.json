{
  "name": "imanfinancial",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:cf": "wrangler pages dev --port 8788 -- npm run dev",
    "build": "astro build",
    "preview": "astro preview",
    "preview:cf": "astro build && wrangler pages dev ./dist --port 8788",
    "astro": "astro"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^6.1.1",
    "tailwindcss": "^4.2.4"
  },
  "devDependencies": {
    "wrangler": "^4.90.1"
  }
}
